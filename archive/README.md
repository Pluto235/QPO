# Archive

该目录保存当前不作为主分析入口、但仍需保留以维护科研可追溯性的文件。

- `notebooks/`：原始探索版 notebook，保留实验性单元、旧尝试和未收敛的显著性原型。
- `bundles/`：历史打包文件，避免继续混在项目根目录。
- `compat/`：已下线的兼容目录与保留用历史入口。

这些文件默认不参与主 workflow；若需要追查历史判断或恢复旧试验路径，再从此处回溯。
